version: '3.8'

services:
  magnet-drive:
    build: .
    ports:
      - "2266:2266"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: magnet-drive-app

  # Optional: Add a reverse proxy if needed
  # nginx-proxy:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./proxy.conf:/etc/nginx/nginx.conf
  #   depends_on:
  #     - magnet-drive